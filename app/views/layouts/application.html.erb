<!DOCTYPE html>
<html>
  <head>
    <title>Inter de Lima</title>
    <%= favicon_link_tag "logo_oscuro.png"%>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/swiper@9/swiper-element-bundle.min.js"></script>
  </head>
  <%= render 'layouts/navbar' %>
  <!--flex col y min h screen para tener el footer siempre al final-->
  <body class="bg-neutral-900 text-white flex flex-col min-h-screen"> 
      <% flash.each do |name, msg| %>
        <ul>
          <li class="bg-blue-100 text-blue-700 p-4 rounded-md top-1 mx-auto w-1/4 mt-9 float"><%= msg %></li>
        </ul>
      <% end %>      
      <%= yield %>
  </body>
  <%= render 'layouts/footer' %>
</html>
