import { Controller as l } from "@hotwired/stimulus";
import { throttle as s } from "lodash";
class o extends l {
  initialize() {
    this.scroll = this.scroll.bind(this);
  }
  connect() {
    this.throttleDelayValue > 0 && (this.scroll = s(this.scroll, this.throttleDelayValue)), window.addEventListener("scroll", this.scroll, { passive: !0 }), this.scroll();
  }
  disconnect() {
    window.removeEventListener("scroll", this.scroll);
  }
  scroll() {
    const t = document.documentElement.scrollHeight - document.documentElement.clientHeight, e = window.scrollY / t * 100;
    this.element.style.width = `${e}%`;
  }
}
o.values = {
  throttleDelay: {
    type: Number,
    default: 15
  }
};
export {
  o as default
};
